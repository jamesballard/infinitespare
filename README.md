Infinite Rooms Server Setup
====

Install with:
echo 'machine github.com login GITHUB_USER password GITHUB_PASS' >> $HOME/.netrc
curl -Ln https://github.com/jamesballard/infinitespare/raw/master/install.sh | /bin/sh

